# Agenda
- Discuss Re-Presenting feature.
	- Should users be able to move to re-present using the existing controls?
	- Discuss the message copy for the control box.
- Discuss security findings
- Discuss findings from forms task:
	- Many forms rely heavily on frontend validation, while backend validation is incomplete.
	- Database updates are not atomic, meaning if they fail halfway through they can leave the app in a broken state.
	- Error Messages Sometimes Expose Internal Information'
	- User Input Is Rendered in Unsafe Contexts (CSVs, Emails, HTML etc)
	- Provider email decision links and some routes return different responses depending on:
		-  Whether a UUID exists
		- Whether a status is valid
		- Whether an action succeeded
	- raw exception messages are being rendered in places.
- Discuss findings from the other security task:
	- The system relies a lot on general permissions rather than tenant scoping.
		- Tenant scoping applied inconsistantly
		- Some allow cross-tenant actions
		- Some filters fail open instead of fail closed.
		- Some email links are not secured.
- Discuss those public action links and what they are used for.
# Minutes