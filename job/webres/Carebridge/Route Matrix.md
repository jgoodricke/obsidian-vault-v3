# Matrix

| #   | route                                                | Protocol | controller action                                    | permission mechanism                                                                                                                                                                                                                                                   | ownership enforcement                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | tenant scoping                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | risk level | remediation recommendation                                                                                                                                                                                                                                                                                                                                                               |
| --- | ---------------------------------------------------- | -------- | ---------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | /login                                               | get      | AuthenticatedSessionController@create                | `guest` middleware only; no role/permission gate                                                                                                                                                                                                                       | N/A, no model/resource access                                                                                                                                                                                                                                                                                                                                                                                                                                                              | None                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | 3. Low     | Keep guest-only access; no direct object lookup/IDOR surface on this action.                                                                                                                                                                                                                                                                                                             |
| 2   | /login                                               | post     | AuthenticatedSessionController@store                 | `guest` middleware + `LoginRequest::authenticate()`; no Spatie permission check; requires active user and either admin role or matching portal; rate limited                                                                                                           | N/A, authenticates submitted credentials rather than loading a user-owned resource                                                                                                                                                                                                                                                                                                                                                                                                         | Portal-level only via `$user->portal === config('app.flavor')`; admins bypass; no `company_id` tenant check                                                                                                                                                                                                                                                                                                                                                                         | 3. Low     | No direct object reference to exploit, but make tenant isolation explicit if required by enforcing organisation/company scope instead of portal-only matching and reviewing the admin bypass.                                                                                                                                                                                            |
| 3   | /forgot-password                                     | get      | PasswordResetLinkController@create                   | `guest` middleware only; no role/permission gate                                                                                                                                                                                                                       | N/A, no model/resource access                                                                                                                                                                                                                                                                                                                                                                                                                                                              | None                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | 3. Low     | Keep guest-only access; no direct object lookup/IDOR surface on this view action.                                                                                                                                                                                                                                                                                                        |
| 4   | /forgot-password                                     | post     | PasswordResetLinkController@forgotPassword           | `guest` middleware + email format validation; no role/permission gate                                                                                                                                                                                                  | None; action looks up arbitrary `User` by submitted email and sends a reset token if found                                                                                                                                                                                                                                                                                                                                                                                                 | None; no portal/company filtering before `User::where('email', ...)`                                                                                                                                                                                                                                                                                                                                                                                                                | 2. Medium  | Return a generic success response for both existing and non-existing accounts, add explicit throttling, and scope lookup to the intended portal/organisation if resets must be tenant-bound.                                                                                                                                                                                             |
| 5   | /reset-password/{token}                              | get      | NewPasswordController@create                         | `guest` middleware + custom token validation via `PasswordResetTokenService::getUserByEmailAndToken(null, $token)`                                                                                                                                                     | Token possession only; no user/session ownership check                                                                                                                                                                                                                                                                                                                                                                                                                                     | None; token is accepted without company/portal context and without email binding in the route                                                                                                                                                                                                                                                                                                                                                                                       | 2. Medium  | Bind the reset page to both email and token, hash stored tokens, and prefer the framework password broker flow to avoid token-only direct access patterns.                                                                                                                                                                                                                               |
| 6   | /reset-password-request/{email}/{displayFirstLogin?} | get      | NewPasswordController@requestSent                    | `guest` middleware + email validation + existence of any unexpired reset token for that email                                                                                                                                                                          | None; action accepts arbitrary email in the URL and checks whether that email currently has an active reset token                                                                                                                                                                                                                                                                                                                                                                          | None; no portal/company filtering on the email lookup                                                                                                                                                                                                                                                                                                                                                                                                                               | 2. Medium  | Stop putting account email in the URL, use flash/session state or a signed opaque reference instead, and avoid exposing whether a reset token currently exists for a given account.                                                                                                                                                                                                      |
| 7   | /reset-password                                      | post     | NewPasswordController@resetPassword                  | `guest` middleware + custom validation of `email`/`token`/`password`; no role/permission gate                                                                                                                                                                          | Token possession only; password is reset for the user linked to the submitted email/token pair                                                                                                                                                                                                                                                                                                                                                                                             | None; no portal/company scoping beyond the email/token match                                                                                                                                                                                                                                                                                                                                                                                                                        | 1. High    | Replace this custom reset path with Laravel `Password::reset()` or, at minimum, invalidate/delete the token after use, rotate `remember_token`, emit `PasswordReset`, and hash reset tokens at rest.                                                                                                                                                                                     |
| 8   | /reset-password-store                                | post     | NewPasswordController@store                          | `guest` middleware + Laravel `Password::reset()` broker validation; no role/permission gate                                                                                                                                                                            | Token possession only through framework reset broker                                                                                                                                                                                                                                                                                                                                                                                                                                       | None; no explicit organisation scoping beyond the email/token pair                                                                                                                                                                                                                                                                                                                                                                                                                  | 3. Low     | Prefer this framework-backed reset path over the custom `/reset-password` action and consolidate on a single reset implementation with consistent tenant rules if required.                                                                                                                                                                                                              |
| 9   | /confirm-password                                    | get      | ConfirmablePasswordController@show                   | `auth` middleware only; no role/permission gate                                                                                                                                                                                                                        | N/A, no arbitrary model/resource access                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Inherited from the authenticated session; no explicit organisation check in the action                                                                                                                                                                                                                                                                                                                                                                                              | 3. Low     | Keep this session-bound; no direct object lookup/IDOR surface on the form render.                                                                                                                                                                                                                                                                                                        |
| 10  | /confirm-password                                    | post     | ConfirmablePasswordController@store                  | `auth` middleware + current-user password validation via `Auth::guard('web')->validate()`; no role/permission gate                                                                                                                                                     | Strong current-user binding: email comes from `$request->user()->email`, so the request cannot target another user account                                                                                                                                                                                                                                                                                                                                                                 | Inherited from the authenticated session; no additional portal/company scoping in the action                                                                                                                                                                                                                                                                                                                                                                                        | 3. Low     | Current-user binding is sound; add throttling if this endpoint needs extra protection against repeated password guessing within an authenticated session.                                                                                                                                                                                                                                |
| 11  | /password                                            | put      | PasswordController@update                            | `auth` middleware + request validation requiring `current_password` and confirmed new password; no separate role/permission gate                                                                                                                                       | Strong current-user binding: updates `$request->user()` only after verifying the current password                                                                                                                                                                                                                                                                                                                                                                                          | Inherited from the authenticated session; no additional portal/company scoping in the action                                                                                                                                                                                                                                                                                                                                                                                        | 3. Low     | Current-user binding is sound; keep using session-derived user context and consider session rotation or broader session invalidation after password change if that is a security requirement.                                                                                                                                                                                            |
| 12  | /logout                                              | post     | AuthenticatedSessionController@destroy               | `auth` middleware only; no role/permission gate; action calls `Auth::guard('web')->logout()`                                                                                                                                                                           | Strong current-session binding: logs out the authenticated session only and does not accept a target user or resource identifier                                                                                                                                                                                                                                                                                                                                                           | Inherited from the authenticated session; no additional organisation/company scoping in the action                                                                                                                                                                                                                                                                                                                                                                                  | 3. Low     | Current-user/session binding is sound; keep this CSRF-protected and session-scoped logout flow, with no direct object reference/IDOR surface to remediate.                                                                                                                                                                                                                               |
| 13  | /health-check                                        | get      | Closure                                              | Public route; no middleware, role, or permission check                                                                                                                                                                                                                 | N/A, no model/resource access and no user-targetable identifier                                                                                                                                                                                                                                                                                                                                                                                                                            | None                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | 3. Low     | Keep this as a minimal liveness endpoint only; do not add tenant-specific state, internal diagnostics, or resource identifiers to the response body.                                                                                                                                                                                                                                     |
| 14  | /invitation/{token}                                  | get      | Closure                                              | Public route; no middleware, role, or permission check                                                                                                                                                                                                                 | None; the `{token}` route parameter is not dereferenced and no invitation/user/resource is loaded server-side                                                                                                                                                                                                                                                                                                                                                                              | None; no portal/company/invitation scoping is applied                                                                                                                                                                                                                                                                                                                                                                                                                               | 3. Low     | Current implementation has no direct object lookup, but it also does not validate or redeem the invitation. If this route is meant to process invitations, replace the closure with a controller that validates a signed or random one-time token, binds it to the intended tenant/account, and expires it after use; otherwise remove the unused `{token}` parameter.                   |
| 15  | /2fa                                                 | get      | TwoFAController@index                                | `auth` middleware only; no role/permission gate; accessible before the `2fa` middleware is satisfied                                                                                                                                                                   | Strong current-user binding: looks up `UserCode` with `auth()->user()->id` and does not accept any user-controlled target identifier                                                                                                                                                                                                                                                                                                                                                       | Inherited from the authenticated session; no explicit portal/company scoping in the action                                                                                                                                                                                                                                                                                                                                                                                          | 3. Low     | Current-user binding avoids IDOR, but the action should not expose `twofa_code` to the client even in non-production, and it should handle missing `UserCode` records safely instead of assuming one exists.                                                                                                                                                                             |
| 16  | /2fa                                                 | post     | TwoFAController@store                                | `auth` middleware only; no role/permission gate; validates presence of `two_factor_code` and checks it against the current user's recent `UserCode` record                                                                                                             | Strong current-user binding: lookup is constrained to `UserCode::where('user_id', auth()->user()->id)` and completion sets `Session::put('user_2fa', $user->id)` for that same session                                                                                                                                                                                                                                                                                                     | Inherited from the authenticated session; no explicit portal/company scoping in the action                                                                                                                                                                                                                                                                                                                                                                                          | 3. Low     | Current-user binding avoids IDOR, but add rate limiting or attempt throttling for 2FA guesses, invalidate codes after successful use, and consider hashing stored 2FA codes if they persist in the database.                                                                                                                                                                             |
| 17  | /2fa/reset                                           | get      | TwoFAController@resend                               | `auth` middleware only; no role/permission gate; accessible before the `2fa` middleware is satisfied                                                                                                                                                                   | Strong current-user binding: regenerates a code only for `auth()->user()` via `generateCode()` and does not accept a target user identifier                                                                                                                                                                                                                                                                                                                                                | Inherited from the authenticated session; no explicit portal/company scoping in the action                                                                                                                                                                                                                                                                                                                                                                                          | 3. Low     | Current-user binding avoids IDOR, but add resend throttling/rate limits, audit repeated resend attempts, and ensure `generateCode()` invalidates prior codes so old and new codes cannot be used in parallel.                                                                                                                                                                            |
| 18  | /applications-enquiry/{status}/{uuid}/{decision}     | get      | ApplicationController@providerStatusDecision         | Public route; no auth, role, or permission middleware; only validates that `{status}` is in `EnquiryHistory::PROVIDER_EMAIL_DECISION_STATUSES` and `{decision}` is `yes`/`no`                                                                                          | Bearer-link only: the action updates state for `Enquiry::where('uuid', $uuid)->first()` with no authenticated user check, no facility/company ownership validation, and no recipient binding beyond possession of the URL                                                                                                                                                                                                                                                                  | None; no portal/company/facility scoping is enforced before mutating the enquiry                                                                                                                                                                                                                                                                                                                                                                                                    | 1. High    | Replace this state-changing GET link with a signed, expiring, one-time action token tied to the specific enquiry and intended tenant/recipient, require a POST for the actual mutation, and record/reject replayed links after first use.                                                                                                                                                |
| 19  | /enquiry-acknowledge/{status}/{uuid}/{decision}      | get      | ApplicationController@providerEnquiryAcknowledgement | Public route; no auth, role, or permission middleware; only validates that the UUID exists, `{status}` is in `EnquiryHistory::PROVIDER_EMAIL_DECISION_STATUSES`, and `{decision}` is `yes`/`no`                                                                        | Bearer-link only: loads `Enquiry::where('uuid', $uuid)->first()` and related application details with no authenticated user check, no facility/company ownership validation, and no recipient binding beyond possession of the URL                                                                                                                                                                                                                                                         | None; no portal/company/facility scoping is enforced before disclosing enquiry/application details                                                                                                                                                                                                                                                                                                                                                                                  | 2. Medium  | Replace the raw UUID link with a signed, expiring, one-time token bound to the intended recipient and tenant, minimize the data shown on the acknowledgement page, and avoid exposing application details to anyone who can guess or obtain the URL.                                                                                                                                     |
| 20  | /profile                                             | get      | Closure                                              | `auth` + `2fa` middleware; no separate role/permission gate                                                                                                                                                                                                            | N/A, no direct model/resource lookup or target identifier; action only renders the profile view for the authenticated session                                                                                                                                                                                                                                                                                                                                                              | Inherited from the authenticated session; no explicit portal/company scoping in the closure                                                                                                                                                                                                                                                                                                                                                                                         | 3. Low     | Keep this session-bound render behind `auth` and `2fa`; there is no direct object reference to exploit unless the page later starts loading arbitrary user records by request parameter.                                                                                                                                                                                                 |
| 21  | /                                                    | get      | IndexController@index                                | `auth` + `2fa` middleware; redirect target chosen from the authenticated user's role/system role via `RouteServiceProvider::redirectToHome()`; no separate permission gate on the action itself                                                                        | Strong current-session binding: action uses `auth()->user()` only and does not accept a target identifier or load an arbitrary user-owned resource                                                                                                                                                                                                                                                                                                                                         | Inherited from the authenticated session; no explicit portal/company scoping in the action, only role-based landing-page selection                                                                                                                                                                                                                                                                                                                                                  | 3. Low     | Current flow has no direct object reference/IDOR surface; keep redirect decisions derived from the session user only, and ensure downstream landing routes enforce their own role and tenant checks rather than relying on this redirect.                                                                                                                                                |
| 22  | /home                                                | get      | HomeController@index                                 | `auth` + `2fa` middleware; no separate route-level permission gate; action allows only users with a system role to see the dashboard and redirects others to `/map`                                                                                                    | Current-user binding is strong for the controller entrypoint: it uses `auth()->user()` only; dashboard counts/notifications are derived from that user, and provider counts are constrained to the user's facilities. The dashboard list helpers rely on service-level permission/company/facility filters rather than an arbitrary request identifier                                                                                                                                     | Yes, partially. `DashboardRepository` scopes hospital metrics to `created_by_id = $user->id`, provider metrics to the user's facility IDs, and list data flows through `ApplicationService`/`EnquiryService`, which apply company/facility filters unless the user has `application-view-all` or `enquiry-view-all`                                                                                                                                                                 | 3. Low     | Keep all dashboard reads derived from the authenticated user and preserve service-layer company/facility filters; if hospital users should only ever see their own applications, tighten `top_applications` to the same user-level scope as the count queries rather than relying only on `company_id` filtering.                                                                        |
| 23  | /failed-jobs                                         | get      | FailedJobController@index                            | `auth` + `2fa` middleware inherited from the route group, plus `can:failed-job-view`; access is permission-gated rather than parameter-gated                                                                                                                           | None at the record level: once authorized, the action loads all rows from `failed_jobs` and does not enforce per-job ownership or current-user scoping                                                                                                                                                                                                                                                                                                                                     | None in the controller; `DB::table('failed_jobs')->orderBy('failed_at', 'desc')->get()` returns the global failed job list with no portal/company filtering                                                                                                                                                                                                                                                                                                                         | 2. Medium  | Keep this limited to tightly controlled operational roles, and if failed job payloads can contain tenant or sensitive data, add redaction/minimization or explicit tenant-aware filtering rather than exposing the full global queue-failure log to every `failed-job-view` holder.                                                                                                      |
| 24  | /notifications/{notification}/mark-as-read           | post     | NotificationController@markAsRead                    | `auth` + `2fa` middleware inherited from the route group; no separate role/permission gate; object access is enforced in-controller after implicit route model binding                                                                                                 | Strong per-record ownership check: the action aborts with `403` unless `$notification->user_id === auth()->id()`, so a user cannot mark another user's notification as read even if they guess the ID                                                                                                                                                                                                                                                                                      | No explicit portal/company scope on the query; effective scoping is user-level ownership only, and the initial route model binding resolves the notification globally before the ownership check runs                                                                                                                                                                                                                                                                               | 3. Low     | Current owner check prevents IDOR, but harden the lookup by scoping the route binding/query to `auth()->id()` or using an explicit current-user query so cross-user IDs return `404` instead of revealing object existence through a `403`.                                                                                                                                              |
| 25  | /users/{id?}                                         | get      | UserController@index                                 | `auth` + `2fa` middleware inherited from the route group, plus in-controller `hasPermissionTo('View create edit users')`; no narrower role gate on the route itself                                                                                                    | No per-record ownership enforcement. Once the permission check passes, the action lists all non-removed users via `UserListRepository`, and if `{id}` is present it loads `User::find($id)` directly for editing                                                                                                                                                                                                                                                                           | None in the current implementation. `UserListRepository` does not filter by `company_id`, and `companyFacilityList()` returns all companies/facilities, so authorized users can view/edit user data across tenants                                                                                                                                                                                                                                                                  | 2. Medium  | If this screen should be tenant-bounded for non-system administrators, add explicit company/portal scoping to both the list query and `User::find($id)` lookup, and limit the company/facility selector to the caller's allowed tenant set instead of returning global options.                                                                                                          |
| 26  | /users                                               | post     | UserController@store                                 | `auth` + `2fa` middleware inherited from the route group, plus in-controller `hasPermissionTo('View create edit users')`; no narrower route-level role gate                                                                                                            | No ownership enforcement in the create path; the action creates a brand-new `User` from request JSON and trusts caller-supplied `company_ids`, `facility_ids`, and `role_id` after only basic shape validation                                                                                                                                                                                                                                                                             | None in the current implementation. `companyFacilityList()` is global, `storeRecord()` attaches arbitrary submitted companies/facilities, and role assignment is limited only by `is_numeric($data['role_id'])` rather than the logged-in user's tenant or role scope                                                                                                                                                                                                               | 1. High    | Add server-side authorization that restricts company/facility assignments and allowable roles to the caller's permitted tenant scope, validate that each submitted company/facility is in that scope before persisting, and enforce a role hierarchy or explicit policy so non-admin users cannot create cross-tenant or over-privileged accounts by tampering with the request payload. |
| 27  | /users/{id}                                          | put      | UserController@update                                | `auth` + `2fa` middleware inherited from the route group, plus in-controller `hasPermissionTo('View create edit users')`; no narrower route-level role gate                                                                                                            | Weak. The action looks up the target record with `User::find($id)` and updates it directly once the caller has the generic permission; there is no per-record ownership check, no policy gate on the specific user, and no restriction preventing edits to users outside the caller's tenant scope                                                                                                                                                                                         | None in the current implementation. The target user is resolved globally by ID, and `storeRecord()` will detach and replace company, facility, and role assignments from caller-supplied input without checking that those assignments or the target user belong to the caller's permitted tenant boundary                                                                                                                                                                          | 1. High    | Replace the generic permission check with per-user authorization/policies, scope the initial user lookup to the caller's allowed tenant set, and validate every submitted company, facility, and role change against that same scope so a user cannot update arbitrary cross-tenant accounts or elevate privileges by tampering with the payload.                                        |
| 28  | /user-profile                                        | put      | UserController@userProfile                           | `auth` + `2fa` middleware inherited from the route group; no separate role/permission gate because this is a self-service profile action                                                                                                                               | Strong current-user binding: the action always updates `auth()->user()` and passes the authenticated user's own ID into validation, so the request cannot target another user by supplying an arbitrary identifier                                                                                                                                                                                                                                                                         | Inherited from the authenticated session. With `isProfile = true`, `storeRecord()` strips company/facility assignment fields and skips role-sync logic, so the action does not reassign tenant memberships or roles                                                                                                                                                                                                                                                                 | 3. Low     | Current-user binding is sound; keep the update path self-scoped, and if profile changes like email/password are considered sensitive, require current-password re-authentication or step-up confirmation for those field changes rather than relying only on the active session.                                                                                                         |
| 29  | /users/{id}                                          | delete   | UserController@destroy                               | `auth` + `2fa` middleware inherited from the route group, plus in-controller `hasPermissionTo('View create edit users')`; no narrower route-level role gate                                                                                                            | Weak. The action resolves the target record with `User::find($id)` and soft-deletes it by setting `removed = true`; there is no per-record ownership check, no policy gate on the specific user, and no restriction preventing deletion of users outside the caller's tenant scope                                                                                                                                                                                                         | None in the current implementation. The target user is looked up globally by ID with no company/portal scoping before the delete, so any caller with the generic permission can soft-delete cross-tenant accounts if they know or can enumerate IDs                                                                                                                                                                                                                                 | 1. High    | Replace the generic permission check with per-user authorization/policies, scope the initial user lookup to the caller's allowed tenant set, and add explicit guardrails for privileged/system accounts so non-admin users cannot soft-delete arbitrary cross-tenant or higher-privilege users by tampering with the route parameter.                                                    |
| 30  | /map                                                 | get      | MapController@index                                  | `auth` + `2fa` middleware inherited from the route group, plus in-controller `hasPermissionTo('Search for available beds')`; no narrower route-level policy gate                                                                                                       | Mixed. General map search does not target a user-owned record, but if `application_uuid` is supplied the action loads `Application::where('uuid', $applicationUuid)->first()` and discloses application context. The intended ownership check is ineffective because `Application::userCanAccess()` currently returns `true` unconditionally                                                                                                                                               | Facility/vacancy search is effectively global in map mode: the controller forces `mapList = true`, and `FacilityMapListRepository` explicitly skips `company_ids` filtering when `mapList` is true. The `application_uuid` lookup also has no real company/portal scoping beyond the stubbed `userCanAccess()` method                                                                                                                                                               | 1. High    | Implement a real `Application::userCanAccess()` policy tied to the caller's tenant/role scope before exposing any application-linked context, and if facility search is meant to be tenant-bounded for some roles, stop bypassing `company_ids` in map mode or derive allowed company/facility filters from the authenticated user rather than from open request context.                |
| 31  | /companies/{id?}                                     | get      | CompanyController@index                              | `auth` + `2fa` middleware inherited from the route group, plus in-controller `hasPermissionTo('Company List')`; elevated roles (`Super Admin`, `System Administrator`, `Account Manager`) are allowed broader access by controller logic                               | Partial per-record enforcement. For most roles, the list query is restricted to the caller's assigned companies and the optional `{id}` edit target is loaded only if it is in that same assigned-company set; elevated roles bypass that restriction and can load any company by ID                                                                                                                                                                                                       | Yes, partially. `CompanyListRepository` applies `whereIn('id', $company_ids)` when the controller derives `company_ids` from the logged-in user's company assignments, but the elevated-role bypass removes tenant scoping entirely for those roles                                                                                                                                                                                                                                 | 3. Low     | Current scoping is reasonable if elevated roles are intentionally cross-tenant. If not, replace the role-based bypass with explicit policies, and make the allowed company set an authorization decision rather than a controller-side role list so list and detail access stay consistent and auditable.                                                                                |
| 32  | /companies                                           | post     | CompanyController@store                              | `auth` + `2fa` middleware inherited from the route group, plus in-controller `hasPermissionTo('Company List')`; notably, creation is not gated by the stronger `Company Add Edit Delete` permission used for update/delete                                             | N/A for existing-object ownership: this action creates a new `Company` rather than dereferencing a target record, so there is no classic direct object reference to an existing tenant object                                                                                                                                                                                                                                                                                              | None in the current implementation. `CompanyService::storeRecord()` persists caller-supplied company attributes, including `portal` and `type_id`, without checking that the logged-in user is allowed to create companies for that portal or tenant scope                                                                                                                                                                                                                          | 2. Medium  | Tighten the permission gate so create requires an explicit company-create permission, and add server-side authorization around allowed `portal`, `type_id`, and any other tenant-defining fields so users with listing access cannot create arbitrary cross-tenant companies by tampering with the payload.                                                                              |
| 33  | /companies/{id}                                      | post     | CompanyController@update                             | `auth` + `2fa` middleware inherited from the route group, plus in-controller `hasPermissionTo('Company Add Edit Delete')`; no narrower route-level policy gate                                                                                                         | Weak. The action resolves the target company with `Company::find($id)` and updates it directly once the caller has the generic company-edit permission; there is no per-record ownership check and no policy gate on the specific company                                                                                                                                                                                                                                                  | None in the current implementation. The target company is looked up globally by ID, and `CompanyService::storeRecord()` persists caller-supplied fields such as `portal` and `type_id` without checking that the company or requested changes are within the caller's permitted tenant scope                                                                                                                                                                                        | 1. High    | Add per-company authorization/policies, scope the initial company lookup to the caller's allowed tenant set, and validate tenant-defining fields like `portal` and `type_id` against that same scope so users with broad edit permission cannot modify arbitrary cross-tenant companies by tampering with the route parameter or payload.                                                |
| 34  | /companies/{id}                                      | delete   | CompanyController@destroy                            | `auth` + `2fa` middleware inherited from the route group, plus in-controller `hasPermissionTo('Company Add Edit Delete')`; no narrower route-level policy gate                                                                                                         | Weak. The action resolves the target company with `Company::find($id)` and soft-deletes it by setting `removed = true`; there is no per-record ownership check and no policy gate on the specific company                                                                                                                                                                                                                                                                                  | None in the current implementation. The target company is looked up globally by ID with no portal/company-bound authorization before the delete, so any caller with the generic company-edit/delete permission can soft-delete cross-tenant companies if they know or can enumerate IDs                                                                                                                                                                                             | 1. High    | Add per-company authorization/policies, scope the initial company lookup to the caller's allowed tenant set, and add explicit safeguards for high-impact or system-critical companies so users with broad edit/delete permission cannot soft-delete arbitrary cross-tenant records by tampering with the route parameter.                                                                |
| 35  | /api/companies/by-type/{type}                        | get      | CompanyController@getByType                          | `auth` + `2fa` middleware inherited from the route group only; unlike the main company endpoints, this action does not perform an in-controller `hasPermissionTo(...)` check or policy check before returning data                                                     | None. The action does not bind results to the current user or an owned record; any authenticated user can query arbitrary company types and optionally pass `exclude_id` to shape the global result set                                                                                                                                                                                                                                                                                    | None. It runs `Company::where('type_id', $type)` and returns matching company `id` and `name` values across the whole table, with no portal, assigned-company, or `removed` filter                                                                                                                                                                                                                                                                                                  | 2. Medium  | Add an explicit permission/policy gate, scope the query to the caller's allowed tenant set or portal, and avoid exposing globally enumerable company IDs to users who should only see tenant-local options.                                                                                                                                                                              |
| 36  | /facilities/{id?}/{vacancy?}                         | get      | FacilityController@index                             | `auth` + `2fa` middleware inherited from the route group, plus in-controller `hasPermissionTo('Facility List')`; no route-level policy gate                                                                                                                            | Partial. For ordinary users, both the facility list query and optional `{id}` lookup are constrained to the companies attached to the user via `user_groups`; however, `super admin`, `system administrator`, and `account manager` roles bypass that filter entirely and may load any facility by ID                                                                                                                                                                                      | Partial. Non-privileged users are scoped by `company_ids` merged into the search request and by `whereIn('company_id', $searchProviderId)` on the selected facility lookup; privileged roles set `company_ids` to `null`, so there is no portal- or tenant-bound restriction for them                                                                                                                                                                                               | 2. Medium  | Add an explicit policy for viewing facilities, scope privileged access to an approved tenant or portal set instead of using a blanket bypass, and ensure the `{id}` detail lookup is authorized against the same tenant rules as the list query so cross-tenant facility enumeration depends on deliberate policy rather than broad role membership.                                     |
| 37  | /facilities                                          | post     | FacilityController@store                             | `auth` + `2fa` middleware inherited from the route group, plus in-controller `hasPermissionTo('Facility Add Edit Delete')`; no route-level policy gate                                                                                                                 | None. The action authorizes only on the broad facility-edit permission, then accepts request JSON and creates a new `Facility` with the submitted `company_id`; there is no check that the target company belongs to, or is manageable by, the caller                                                                                                                                                                                                                                      | None in the current implementation. Although the facility page only preloads company options scoped to the logged-in user for non-privileged roles, the POST handler validates `company_id` as merely numeric and mass-assigns it directly, so a caller can tamper with the payload to create facilities under arbitrary companies                                                                                                                                                  | 1. High    | Authorize creation against the target company, validate `company_id` with an `exists` rule plus tenant-aware ownership checks, and derive or constrain the allowable company set server-side rather than trusting the client-visible company dropdown to enforce tenant boundaries.                                                                                                      |
| 38  | /facilities/{id}                                     | put      | FacilityController@update                            | `auth` + `2fa` middleware inherited from the route group, plus in-controller `hasPermissionTo('Facility Add Edit Delete')`; no route-level policy gate                                                                                                                 | None. The action authorizes only on the broad facility-edit permission, resolves the target facility globally via `Facility::find($id)`, and then updates it without checking whether the caller is allowed to manage that specific record                                                                                                                                                                                                                                                 | None in the current implementation. There is no `company_id` or portal scoping on the initial lookup, and the submitted payload may include a different numeric `company_id`, which is mass-assigned to the facility, allowing both cross-tenant edits and reassignment of a facility to another company                                                                                                                                                                            | 1. High    | Add record-level authorization for the target facility, scope the initial lookup to the caller's permitted tenant set, and prevent arbitrary company reassignment by validating the destination `company_id` against tenant-aware rules or deriving it server-side from authorized context.                                                                                              |
| 39  | /facilities/{id}                                     | delete   | FacilityController@destroy                           | `auth` + `2fa` middleware inherited from the route group, plus in-controller `hasPermissionTo('Facility Add Edit Delete')`; no route-level policy gate                                                                                                                 | None. The action authorizes only on the broad facility-edit/delete permission, resolves the target facility globally via `Facility::find($id)`, and soft-deletes it without checking whether the caller is allowed to manage that specific record                                                                                                                                                                                                                                          | None in the current implementation. The delete path has no `company_id`, portal, or assigned-company scoping before marking the facility as removed, so a caller with the generic permission can soft-delete facilities across tenants if they know or can enumerate IDs                                                                                                                                                                                                            | 1. High    | Add record-level authorization for deleting facilities, scope the initial facility lookup to the caller's permitted tenant set, and reserve broad delete rights for explicitly approved cross-tenant admin roles with auditable safeguards rather than a single generic permission check.                                                                                                |
| 40  | /vacancies                                           | get      | VacancyController@index                              | `auth` + `2fa` middleware inherited from the route group, plus in-controller `hasPermissionTo('Vacancy List')`; no route-level policy gate                                                                                                                             | N/A in the current implementation. The action does not load a specific vacancy or facility record and instead halts with `dd($searchProviderId, $request->all())` after computing the caller's allowed company IDs                                                                                                                                                                                                                                                                         | Partial in intent only. Non-privileged users derive `company_ids` from their assigned companies, while `super admin`, `system administrator`, and `account manager` roles bypass that restriction with `null`; however, the method never executes a vacancy query, so these tenant rules are not actually enforced on returned data                                                                                                                                                 | 3. Low     | Remove the `dd()` debug stop, implement the vacancy listing using the same tenant-aware scoping rules as the facility search flow, and add an explicit policy or query constraint so any eventual vacancy dataset is filtered server-side by the caller's permitted companies rather than by UI assumptions.                                                                             |
| 41  | /facilities/{facility}/vacancy                       | put      | VacancyController@update                             | `auth` + `2fa` middleware inherited from the route group, plus in-controller `hasPermissionTo('Update bed vacancies')`; no route-level policy gate                                                                                                                     | Partial. The action binds the target `Facility` from the route and forces each written vacancy record to use that facility's `id` and `company_id`, so the client cannot directly choose a different parent company or facility in the payload; however, there is no check that the caller is allowed to manage the bound facility at all, and the controller itself notes this as a TODO                                                                                                  | None in practice for the target facility. Route model binding resolves `Facility $facility` globally by ID with no `company_id`, assigned-company, or portal constraint, so a user with the generic vacancy-update permission can create or overwrite vacancy records for facilities in other tenants by tampering with the `{facility}` route parameter                                                                                                                            | 1. High    | Authorize the bound facility before any vacancy write, scope facility resolution to the caller's permitted tenant set, and keep deriving `facility_id`/`company_id` server-side so payload tampering cannot move vacancies once proper record-level checks are added.                                                                                                                    |
| 42  | /facilities/{facility}/vacancy/{vacancy?}            | delete   | VacancyController@destroy                            | `auth` + `2fa` middleware inherited from the route group, plus in-controller `hasPermissionTo('Update bed vacancies')`; no route-level policy gate                                                                                                                     | Partial. The action checks that an optional bound vacancy belongs to the bound facility, which prevents deleting an unrelated vacancy once both records are already loaded; however, there is no authorization check that the caller may manage the bound facility itself                                                                                                                                                                                                                  | None in practice for the target facility. Route model binding resolves `Facility $facility` globally by ID with no tenant constraint, and when `{vacancy}` is omitted the action deletes all vacancies for that facility, so a user with the generic vacancy-update permission can remove vacancy data across tenants by tampering with the `{facility}` route parameter                                                                                                            | 1. High    | Authorize the bound facility before any vacancy deletion, scope facility resolution to the caller's permitted tenant set, and consider separating single-record delete from bulk facility-wide delete so mass removal requires a narrower, explicitly authorized capability.                                                                                                             |
| 43  | /facilities/{facility}/vacancy-refresh               | put      | VacancyController@refresh                            | `auth` + `2fa` middleware inherited from the route group, plus in-controller `hasPermissionTo('Update bed vacancies')`; no route-level policy gate                                                                                                                     | None. The action authorizes only on the broad vacancy-update permission and then updates timestamps for the bound facility and all of its vacancies without checking whether the caller is allowed to manage that specific facility                                                                                                                                                                                                                                                        | None in the current implementation. Route model binding resolves `Facility $facility` globally by ID with no tenant constraint, and the action updates both `vacancies.updated_at` and `facilities.refreshed_at` for that facility, so a user with the generic permission can refresh cross-tenant vacancy status by tampering with the `{facility}` route parameter                                                                                                                | 1. High    | Authorize the bound facility before refreshing timestamps, scope facility resolution to the caller's permitted tenant set, and treat this refresh capability as a record-level write operation subject to the same tenant checks as vacancy create/update/delete actions.                                                                                                                |
| 44  | /application-files/{applicationFile}                 | get      | ApplicationFileController@download                   | `auth` + `2fa` middleware inherited from the route group; no route-level `can:application-view` middleware. In-controller `canAccessApplicationFile()` grants access to `application-view-all` users and otherwise applies custom company/facility checks              | Partial. The action authorizes against the parent `$applicationFile->application`, allowing hospital users only when the application belongs to their company and provider users only when the application has an enquiry for one of their assigned facilities; however, it never requires the baseline `application-view` permission that guards the normal application screens                                                                                                           | Partial. Record access is tied to the parent application rather than being completely global, and `application-view-all` remains the only full bypass; however, because this route sits outside the `can:application-view` group and the helper checks only company type plus facility/company linkage, authenticated hospital/provider users without `application-view` can still download linked files if they otherwise satisfy those relationship checks                        | 2. Medium  | Move this route behind `can:application-view` or enforce that permission explicitly in the controller, then centralize the record-level rule in an application or application-file policy so file downloads inherit the same tenant and role checks as application viewing instead of relying on a custom helper alone.                                                                  |
| 45  | /application-files/{applicationFile}                 | delete   | ApplicationFileController@delete                     | `auth` + `2fa` middleware inherited from the route group; no route-level middleware, but in-controller `ApplicationFile::canBeDeletedBy()` requires either `application-file-delete-all` or `application-file-delete`                                                  | Partial. The delete check is record-aware: `application-file-delete-all` grants the intended global bypass, ordinary users must hold `application-file-delete`, and hospital users can otherwise delete files tied to applications owned by their company; however, the creator check runs before any company/application scoping and allows a permitted user to delete any file they originally uploaded                                                                                  | Partial. Most deletions are constrained either by the parent application's `company_id` or by the explicit global `application-file-delete-all` permission, so route model binding is not fully global in practice; however, a user who still has `application-file-delete` can delete files they created even if they no longer belong to the same hospital or no longer have access to the parent application, which lets creator ownership bypass current tenant scoping         | 2. Medium  | Keep the explicit delete permission check, but require the deleter to still satisfy the current parent-application access rule unless they hold `application-file-delete-all`; moving this logic into a policy would let file deletion share the same live tenant/role checks as application viewing instead of relying on stale `created_by_id` ownership alone.                        |
| 46  | /applications                                        | get      | ApplicationController@index                          | `auth` + `2fa` middleware inherited from the route group plus route-level `can:application-view`; the controller delegates list loading to `ApplicationService::search()`, which injects a `company_id` filter unless the user can `application-view-all`              | Partial. Ordinary users are intended to be limited to applications whose `company_id` matches `auth()->user()->company_id`, while `application-view-all` is the explicit global bypass; however, `ApplicationService::getCompanyIdFilter()` falls back to `0` for `application-view` users without an attached company, and `ApplicationRowRepository::search()` treats that falsey value as "no filter"                                                                                   | Partial. Company-backed users are normally tenant-scoped by owning hospital/company rather than by a global query, but the route has no finer provider-facility relationship check and, more importantly, a misconfigured `application-view` user with no company bypasses tenant scoping entirely because the repository skips the `company_id` predicate when it receives `0`, exposing the full application index                                                                | 1. High    | Fail closed when a non-`application-view-all` user has no company, and make the repository apply an explicit deny-all predicate instead of skipping the filter on `0`/null. Moving this into a shared application policy or query scope would make list access consistently tenant-aware and prevent future falsey-filter regressions.                                                   |
| 47  | /test                                                | get      | ApplicationController@test                           | `auth` + `2fa` middleware inherited from the route group plus route-level `can:application-view`; however, the route points to `ApplicationController@test`, and there is no corresponding `test()` method on the controller                                           | None in practice. Because the target action is missing, the request cannot reach any application query or record-loading logic; Laravel will fail when it tries to dispatch the route instead of performing a meaningful ownership check                                                                                                                                                                                                                                                   | None in practice. The route is not currently capable of returning tenant data because it is broken before it reaches the service layer, so there is no working global-bypass path beyond the fact that any user with `application-view` can hit an endpoint that should not exist                                                                                                                                                                                                   | 3. Low     | Remove the dead route if it is obsolete, or implement the missing controller action and give it the same tenant-aware policy/query scoping as the real application endpoints before exposing it to `application-view` users.                                                                                                                                                             |
| 48  | /applications/{id}                                   | get      | ApplicationController@index                          | `auth` + `2fa` middleware inherited from the route group plus route-level `can:application-view`; the controller resolves the detail view through `ApplicationService::find($id)`, which passes a `company_id` filter unless the user can `application-view-all`       | Partial. Ordinary users are intended to load only applications owned by their `company_id`, while `application-view-all` is the explicit global bypass; however, `ApplicationService::getCompanyIdFilter()` returns `0` for `application-view` users without an attached company, and `ApplicationRepository::find()` skips the `applications.company_id` predicate when the filter is falsey                                                                                              | Partial. Company-backed users are usually constrained to their own hospital/company records, but there is no additional provider-facility check on the detail route and a misconfigured `application-view` user with no company can fetch any application by ID because the repository falls back to an unscoped lookup. Once returned, `ApplicationResource` also exposes linked files and enquiries for that application                                                          | 1. High    | Fail closed when a non-`application-view-all` user has no company, and make `ApplicationRepository::find()` enforce an explicit deny-all predicate instead of treating `0`/null as "no tenant filter." Centralizing application access in a policy or reusable scoped query would keep detail views and related resources aligned with the intended tenant boundary.                     |
| 49  | /applications/new                                    | get      | ApplicationController@index                          | `auth` + `2fa` middleware inherited from the route group plus route-level `can:application-new`; however, the route reuses `ApplicationController::index()`, which still calls `ApplicationService::search()` and loads the applications collection for the "new" page | Partial. The route is intended to be a create screen, not a general browse endpoint, and the shared search path still tries to limit ordinary users to their own `company_id` while `application-view-all` remains the explicit global bypass used by privileged roles; however, non-privileged users with `application-new` but no attached company hit the same falsey-`0` bug in `getCompanyIdFilter()` / `ApplicationRowRepository::search()` and receive an unscoped application list | Partial. Hospital users with a real company are generally limited to their own applications, and the allowed `application-new` roles are a subset of existing application viewers, so this route does not create a new role-based bypass; however, because the create page unnecessarily loads the application index, a misconfigured `application-new` user with no company can still enumerate all applications through this route when the repository skips the tenant predicate | 1. High    | Stop loading the application list on the "new" route unless it is strictly required, and fail closed when a non-privileged creator has no company. The same shared application policy/query scope used for the main index should back this page so a create-only screen cannot accidentally become a global read path through a falsey tenant filter.                                    |
| 50  | /applications/new                                    | post     | ApplicationController@store                          | `auth` + `2fa` middleware inherited from the route group plus route-level `can:application-new`; the controller also aborts if the caller has no attached company, then forcibly sets `company_id` and `created_by_id` from the authenticated user before saving       | Partial. New applications themselves are correctly anchored to the caller's own company because `company_id` is overwritten server-side and users without a company are rejected; however, if the request includes `facility_id`, the controller immediately calls `ApplicationService::storeEnquiryRecord()` and creates an enquiry for that facility without validating that the facility exists in the caller's allowed tenant scope                                                    | Partial. The application record cannot be created directly under another company unless the caller already has a privileged role with `application-new`, so the main write path is not globally mass-assignable; however, any ordinary hospital user with `application-new` can attach the new application to an arbitrary facility ID and trigger a cross-tenant enquiry because `facility_id` is taken from the request and written without authorization checks                  | 1. High    | Keep the server-side assignment of `company_id`, but validate and authorize `facility_id` before creating the linked enquiry. The enquiry creation path should enforce the same tenant/policy checks used elsewhere for facility access, or drop inline enquiry creation from this endpoint entirely and require a separately authorized flow.                                           |
| 51  | /applications-enquiry                                | post     | ApplicationController@mapSubmitEnquiry               | `auth` + `2fa` middleware inherited from the route group plus route-level `can:application-new`; the controller also requires `hasPermissionTo('Complete and enter data into the forms')` before proceeding                                                            | None for the submitted target records. The action validates only input shape and duplicate enquiry pairs, then writes `application_id` and each posted `facility_id` directly without resolving the application through a scoped lookup or checking whether the caller may access those facilities                                                                                                                                                                                         | None on the write path. This endpoint bypasses the existing application/enquiry scoping helpers entirely, so a caller can submit enquiries against arbitrary application IDs and facility IDs and can also overwrite map-search metadata for any `application_id` because `storeSearchRecord()` rewrites saved search details without tenant checks                                                                                                                                 | 1. High    | Resolve the target application through the existing scoped lookup before any write, authorize each requested facility against the caller's allowed tenant/facility set, reject out-of-scope IDs, and only then create enquiries or update map-search records.                                                                                                                            |
| 52  | /applications/edit/{id}                              | post     | ApplicationController@update                         | `auth` + `2fa` middleware inherited from the route group plus route-level `can:application-edit`; no policy or in-controller `authorize()` call beyond loading the application through `ApplicationService::find()`                                                    | Partial. Lookup is only company-based, and there is no per-record owner/creator/assignee check. The update path also overwrites `created_by_id` with the current editor, so creator-based ownership is neither preserved nor enforced                                                                                                                                                                                                                                                      | Partial on lookup, absent on write. The route initially scopes the loaded application by `company_id`, but `storeRecord()` mass-assigns raw posted attributes onto a fully fillable `Application`, so a user who can edit an in-scope record can post `company_id` and move it to another tenant, bypassing the dedicated transfer flow                                                                                                                                             | 1. High    | Keep the coarse permission gate, but add record-level authorization for the loaded application and replace the generic mass-assignment path with an explicit allowlist that excludes security-sensitive fields such as `company_id` and `created_by_id`. Any tenant move should go through the dedicated transfer endpoint only.                                                         |
| 53  | /enquiries                                           | get      | EnquiryController@index                              | `auth` + `2fa` middleware inherited from the route group plus route-level `can:enquiry-view`; the controller immediately delegates to `EnquiryService::search()` with no additional policy check                                                                       | Partial, and only query-based. The list path does not call `Enquiry::canAccess()` or any comparable record-level policy; it relies on service/repository filters derived from the current user's company and assigned facilities                                                                                                                                                                                                                                                           | Partial and fail-open. Non-`enquiry-view-all` users are intended to be scoped by provider company and assigned facility IDs, but the repository only applies those predicates when the values are truthy/non-empty. A provider with no assigned facilities expands to company-wide visibility, and a misconfigured `enquiry-view` user with no company and no facilities falls through to a globally unscoped enquiry list                                                          | 1. High    | Fail closed for non-`enquiry-view-all` users who lack a valid tenant context, and centralize enquiry access in a shared policy/query scope that enforces the same company-plus-facility rule as `Enquiry::canAccess()` for both list and detail routes.                                                                                                                                  |
| 54  | /enquiries/{id}                                      | get      | EnquiryController@index                              | `auth` + `2fa` middleware inherited from the route group plus route-level `can:enquiry-view`; the detail view is resolved through `EnquiryService::find()` with no direct `canAccess()` or policy call                                                                 | Partial. The controller depends on query filtering in `EnquiryService::find()` / `EnquiryRepository::find()` rather than the stricter model helper `Enquiry::canAccess()`, so detail access is enforced only if those filters remain in place                                                                                                                                                                                                                                              | Partial and fail-open in edge cases. Users with both company and facility assignments are scoped as intended, but missing facility assignments degrade access to company-wide, and a non-privileged user with neither company nor facilities can fall through to an effectively global `where id = ?` lookup. Once returned, `EnquiryResource` exposes the linked `ApplicationResource`, including rich application data and related files/enquiries                                | 1. High    | Fail closed when tenant context is missing, and enforce detail access through a single shared policy/scope equivalent to `Enquiry::canAccess()` instead of truthy query filters. That keeps detail reads aligned with the stricter company-and-facility rule already present in the model.                                                                                               |
| 55  | /application/{id}/transition/{statusId}              | post     | ApplicationController@transition                     | `auth` + `2fa` middleware inherited from the route group plus route-level `can:application-bulk-update-enquiry-status`; the controller then derives and checks `enquiry-transition-to-{status}-status` for the requested target status                                 | Partial in code shape, but effectively none for real callers. The action only verifies that the parent application can be found through `ApplicationService::find()` and then bulk-updates every child enquiry; it never authorizes each affected enquiry/facility individually                                                                                                                                                                                                            | Global for the roles that can actually hit the route. `application-bulk-update-enquiry-status` is limited to system/super admins, and those roles also have `application-view-all`, so the underlying application lookup is unscoped. The subsequent bulk write also bypasses the normal single-enquiry transition path by using `updateQuietly()`, which likely skips the `HasTransitions` state-machine guard                                                                     | 2. Medium  | Keep the coarse admin-only gate, but move the bulk write into a dedicated service that authorizes the target application and each affected enquiry/facility explicitly, scopes the operation to the intended tenant set, and uses the same transition enforcement and side effects as the single-enquiry status flow instead of `updateQuietly()`.                                       |
| 56  | /applications/{id}/patient-transfer                  | post     | ApplicationController@patientTransfer                | `auth` + `2fa` middleware inherited from the route group plus route-level `can:application-patient-transfer`; that permission is assigned only to `Super Admin`, and the controller performs no additional policy check                                                | Partial in code shape, but effectively none for real callers. The source application is loaded through `ApplicationService::find()`, which is unscoped for the only authorized role because `Super Admin` also has `application-view-all`; the destination is any existing `companies.id`, subject only to “must be a hospital” and “must differ from current company” validation                                                                                                          | Global. The UI helper enumerates all hospitals, and the POST path resolves the destination with an unscoped `Company::findOrFail($company_id)`. There is no same-tenant or policy-based constraint between source and destination companies, so the transfer and notification fan-out are fully cross-tenant by design for the current role set                                                                                                                                     | 2. Medium  | Add an explicit policy/service rule for both the source application and destination company, and scope destination selection to an allowed transfer set instead of every hospital. If cross-tenant transfer is intentionally reserved for global admins, encode that directly rather than relying on the implicit `application-view-all` bypass.                                         |
| 57  | /enquiries/{id}/transition/{statusId}                | post     | EnquiryController@transition                         | `auth` + `2fa` middleware inherited from the route group plus route-level `canAny` over the enquiry transition permissions; the controller then derives `enquiry-transition-to-{status}-status` from the target status and checks that permission again                | Partial. The controller only checks whether `EnquiryService::find()` returns a record; it does not call the stricter `Enquiry::canAccess()` helper, so there is no direct creator/facility-membership authorization at the write point beyond whatever the scoped lookup happens to enforce                                                                                                                                                                                                | Partial and fail-open. The service/repository intend to scope by provider company and assigned facilities, but those filters are conditional. A user with transition permission but no facilities can expand to company-wide access, and a misconfigured user with neither company nor facilities can fall through to an effectively unscoped enquiry lookup before mutating status and history                                                                                     | 1. High    | Move record authorization into a single enquiry policy or shared authorized query and require both the exact transition permission and an explicit record-access rule equivalent to `Enquiry::canAccess()`. The lookup should fail closed for non-privileged users missing company or facility context instead of silently omitting filters.                                             |
| 58  | /enquiry-history/{enquiryHistoryId}/undo             | post     | EnquiryController@undoTransition                     | `auth` + `2fa` middleware inherited from the route group plus the broad enquiry-transition `canAny` route group; there is no undo-specific permission, and the controller relies on `EnquiryHistory::canUndo()` after loading the history row                          | Partial. The action loads the history row globally by ID, then checks access to the parent enquiry through `EnquiryService::find()`, and finally allows undo only if the caller either created the latest history entry or has `enquiry-edit-all`. That is narrower than a pure IDOR, but it is still not expressed as a dedicated policy on the enquiry/history pair                                                                                                                      | Partial and fail-open. The parent-enquiry check inherits the same conditional company/facility scoping as the other enquiry read paths, so a non-global user missing company/facility context can fall through to an unscoped enquiry lookup. The route-level gate is also broader than necessary because any listed transition permission can reach the undo endpoint                                                                                                              | 2. Medium  | Add an explicit undo ability or policy check, load the history row through a tenant-scoped enquiry query rather than raw `EnquiryHistory::findOrFail()`, and make the enquiry lookup fail closed when a non-global user has no valid company/facility scope instead of silently dropping filters.                                                                                        |
| 59  | /requested-enquiries/update-enquiry-status           | post     | ApplicationController@enquiryStatusUpdateByProvider  | `auth` + `2fa` middleware inherited from the route group; no route-level `can:` middleware, but the controller requires `hasPermissionTo('enquiry-edit-status')` and then checks `Enquiry::canAccess()` on the loaded record                                           | Partial by design. There is no creator-based ownership rule, but the endpoint intentionally treats “assigned provider for this facility” as the access boundary: non-admin users must both hold `enquiry-edit-status` and satisfy `Enquiry::canAccess()`, which requires matching company plus assigned facility membership                                                                                                                                                                | Partial. The initial lookup is global via `Enquiry::find($id)`, but ordinary provider users are blocked from cross-tenant writes by the subsequent company-and-facility check in `Enquiry::canAccess()`. `System Administrator` and `Super Admin` still bypass tenant checks through `isAdmin()`, which appears intentional for those roles                                                                                                                                         | 3. Low     | Move the current controller-local rule into an explicit policy and use a scoped enquiry lookup instead of `Enquiry::find()` plus a follow-up access check. If provider status changes are meant to stay narrow, mirror the per-status permission pattern used by the main enquiry transition endpoint.                                                                                   |
| 60  | /long-application                                    | get      | Closure                                              | `auth` + `2fa` middleware inherited from the route group only; the route is a bare closure with no `can:*`, policy, or controller-level authorization, unlike the normal application-create flow which uses `can:application-new`                                      | None. The closure does not load a model or perform any ownership check; it just returns the `Owner/LongApplication/LongApplication` Inertia page                                                                                                                                                                                                                                                                                                                                           | None on this route, but also no server-side data exposure in the current implementation. The page receives no tenant-scoped props and its submit handler is effectively unimplemented, so authenticated users can open an internal form shell without reaching protected application data or writes                                                                                                                                                                                 | 3. Low     | Move `/long-application` behind the same `application-new` permission as the real create flow, or remove the route entirely if it is experimental/dead. Leaving an unguarded internal page reachable by URL is unnecessary even if it is currently only a static shell.                                                                                                                  |
| 61  | /api/type-of-care/{accommodationType}/solo-facility  | get      | TypeOfCareController@showSoloFacility                | `auth` + `2fa` middleware inherited from the route group only; there is no route-level `can:*` middleware, no policy call, and no controller-side authorization beyond route model binding for `AccommodationType`                                                     | None. The controller only counts and fetches facilities matching the submitted accommodation type and never checks the authenticated user's company membership, assigned facilities, or any facility-access helper                                                                                                                                                                                                                                                                         | None. The facility query has no `company_id` or tenant predicate, so any authenticated 2FA user can probe for a single matching facility across all tenants and receive raw facility JSON when exactly one match exists. That bypasses the tighter company-scoping patterns used by the normal facility/application flows                                                                                                                                                           | 2. Medium  | Protect the route with the same business permission as the caller flow, and scope the facility lookup to the caller's allowed companies/facilities using the existing facility-access rules. The response should also be minimized to the small identifier payload the frontend actually needs instead of returning the full facility model.                                             |
